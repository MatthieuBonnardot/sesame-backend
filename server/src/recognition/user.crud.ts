const addFace: Function = async () => {
  // TODO Enable user to add a face to a persons group.
};

const removeFace: Function = async () => {
  // TODO Enable user to remove a face from a persons group
};

const getFacesPerPerson: Function = async () => {
  // TODO Enable user to get their faces from a persons group
};

const deleteFacesPerPerson: Function = async () => {
  // TODO Enable user to delete their faces from a persons group
};

module.exports = {
  addFace,
  removeFace,
  getFacesPerPerson,
  deleteFacesPerPerson,
};
